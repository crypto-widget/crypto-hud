export struct QuoteRow {
    symbol: string,
    price: string,
    change: string,
    positive: bool,
}

export struct PluginMarketItem {
    title: string,
    description: string,
    usage: string,
    status: string,
    available: bool,
    builtin: bool,
    symbol_limit: int,
    preview_kind: int,
    preview_frame_index: int,
    preview_image_count: int,
    preview_image_1: image,
    preview_image_2: image,
    preview_image_3: image,
    preview_image_4: image,
    preview_image_5: image,
}
